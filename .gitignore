# Trang web đã lưu (Save page as) — chỉ giữ bản trích text ở data/btc/
*_files/
*.html

# PDF nguồn — đã chưng cất vào knowledge/, không commit lại (xem PROGRESS 11/09)
/data/*
!/data/btc/

# Thư mục làm việc ngày thi & diễn tập — sinh ra lúc chạy, không commit
# Neo ở gốc repo: `drill/` trần sẽ ẩn cả .claude/skills/drill/
/battle/
/drill/

.DS_Store
